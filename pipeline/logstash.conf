input {
    beats {
        port => 5044
    }
}

filter {
  json {
    source => "message"
	target => "message"
	skip_on_invalid_json => true
  }
}

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		user => elastic
		password => changeme
	}
}